# Copy this file to terraform.tfvars and fill in real values.
# terraform.tfvars is gitignored — never commit real values.

admin_source_ip = "113.211.131.74/32"  # Your public IP in CIDR notation, /32 = single IP